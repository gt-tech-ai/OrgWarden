[project]
name = "orgwarden"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "austin-weeks", email = "austinweeksmusic@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "requests>=2.32.3",
]

[project.scripts]
orgwarden = "orgwarden.__main__:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
